name: "Home Assistant DB Manager"
description: "View and manage Home Assistant database tables (states, statistics, events) for SQLite, MySQL and PostgreSQL"
version: "2026.8.3"
slug: "ha-db-manager"
url: "https://github.com/wrfz/home-assistant-addons"
arch:
  - aarch64
  - amd64
init: false
ingress: true
ingress_port: 8099
panel_icon: "mdi:database-search"
panel_title: "DB Manager"
map:
  - config:ro
startup: application
boot: manual
options:
  log_level: info
schema:
  log_level: list(info|debug)
